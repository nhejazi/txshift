vim_shift <- function(data, delta_grid, est_type = c("tmle", "aipw"), ...) {
  # defaults...

  # call different functions appropriately
  if (est_type == "tmle") {
    ...
  } else if (est_type = "aipw") {
    ...
  }

  # ...

  # output

}
